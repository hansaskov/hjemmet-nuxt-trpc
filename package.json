{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^5.3.5",
    "nuxt": "npm:nuxt3@latest",
    "prisma": "^4.4.0"
  },
  "dependencies": {
    "@prisma/client": "^4.4.0",
    "trpc-nuxt": "^0.3.0",
    "ts-node": "^10.9.1",
    "zod": "^3.19.1"
  },
  "prisma": {
    "schema": "server/database/schema.prisma",
    "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} server/database/seed.ts"
  }
}

